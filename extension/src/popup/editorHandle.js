// The editor-mode controller, held apart from the wiring that builds it (./editorSection.js)
// so the modules that CALL it don't have to import the whole panel to reach it.
export const editorMode = {};
