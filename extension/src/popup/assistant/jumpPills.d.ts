// Shapes for popup/assistant/jumpPills.js — the ⌃/⌄ pills over the transcript's edges.
export declare function createJumpPills(transcriptEl: HTMLElement): {
  /** Re-measure and re-sync — a collapsed (display:none) section measured zero. */
  resync(): void;
};
