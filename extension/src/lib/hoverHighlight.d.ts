/** Outlines the page element matching `source` on `tabId`; false if none was found (or a restricted page). */
export declare function highlightSourceOnTab(tabId: number, source: string, color?: string): Promise<boolean>;
