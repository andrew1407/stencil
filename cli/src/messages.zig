//! The console's user-facing message strings, one named constant each — handlers.zig
//! carries the logic and names the message, not the wording. Same `logo.err/note/print`
//! call shape: tests/pins/*.txt pin what several of these render to, byte for byte.

pub const upload_needs_path = "upload needs a path or URL — e.g. '/upload photo.png' (or copy an image and run a bare '/upload')\n";
pub const source_upload_needs_url = "source-upload needs a URL — e.g. '/source-upload https://example.com'\n";
pub const source_upload_usage = "source-upload takes '<url> [index=0] [format=all] [minW=-1] [maxW=-1] [minH=-1] [maxH=-1]'\n";
pub const scraping = "scraping {s}…\n";
pub const blank_usage = "blank takes '[format] [w h] [color]' (a page format and explicit dims are exclusive) — e.g. '/blank 800 600 white' or '/blank b5 pink'\n";
pub const save_needs_path = "save needs an output path — e.g. '/save out.png' (or a bare '/save' to push to the active server project)\n";
pub const could_not_write_layout = "could not write layout to {s} ({s})\n";
pub const wrote_layout = "wrote {s} (layout)\n";
pub const delete_needs_path = "delete needs a .stencil path — e.g. '/delete project.stencil'\n";
pub const delete_no_urls = "delete only removes local files, not URLs\n";
pub const delete_not_stencil = "delete only removes .stencil project files (got '{s}')\n";
pub const delete_escapes_cwd = "refusing to delete a path that escapes the working directory: '{s}'\n";
pub const could_not_delete = "could not delete {s} ({s})\n";
pub const deleted = "deleted {s}\n";
pub const formulas_state = "formulas {s}: x -> {s}, y -> {s}\n";
pub const formulas_off = "formulas off (expressions kept)\n";
pub const formulas_cleared = "formulas cleared\n";
pub const out_of_memory = "out of memory\n";
pub const invalid_formula = "invalid {c} formula: {s}\n";
pub const formula_usage = "usage: /formula [x|y <expr> | on | off | clear]   (e.g. '/formula x x*2 + 1')\n";
pub const format_custom_needs_dims = "custom takes width + height in cm (0.1–500) — e.g. '/format custom 21 29.7'\n";
pub const format_set_custom = "page format set to custom ({d}×{d}cm)\n";
pub const unknown_page_format = "unknown page format '{s}' — type '/format' to list them\n";
pub const format_takes_one_name = "/format takes one name — e.g. '/format b5' (or '/format custom <w> <h>')\n";
pub const format_set = "page format set to {s} ({d}×{d}cm)\n";

pub const connect_needs_url = "connect needs a server URL — e.g. '/connect http://host:8090 [token]'\n";
pub const already_connected = "already connected to {s}\n";
pub const connected = "connected to {s}\n";
pub const no_server_connections = "no server connections — use '/connect <url>'\n";
pub const disconnected = "disconnected from {s}\n";
pub const not_connected_to = "not connected to {s}\n";
pub const reconnected_count = "reconnected {d}/{d} server(s)\n";
pub const not_connected_connect_first = "not connected to {s} — '/connect' first\n";
pub const reconnect_failed = "reconnect to {s} failed ({s})\n";
pub const reconnected = "reconnected to {s}\n";
pub const connections_usage = "usage: /connections [admin|session]\n";
pub const no_filtered_connections = "no {s} connections (of {d})\n";
pub const connections_count = "connections ({d}):\n";
pub const projects_on = "projects on {s} ({d}):\n";
pub const projects_across = "projects across {d} servers ({d}):\n";
pub const none_row = "  (none)\n";
pub const fetch_hint = "use '/fetch <name>' to open a project\n";
pub const no_active_server_project = "no active server project — '/fetch <name>' first\n";
pub const project_server_disconnected = "the active project's server is not connected — '/reconnect' first\n";
pub const could_not_read_project_color = "could not read the project colour ({s})\n";
pub const invalid_color_or_clear = "invalid colour '{s}' — give a '#rrggbb' / name, or 'clear'\n";
pub const project_color_cleared = "project colour cleared (neutral grey)\n";
pub const could_not_read_blank_color = "could not read the blank colour ({s})\n";
pub const blank_color_not_blank = "blank colour: (this project is not a blank image)\n";
pub const not_a_blank_project = "this project is not a blank image — nothing to recolour\n";
pub const invalid_color = "invalid colour '{s}' — give a '#rrggbb' / name\n";
pub const description_too_long = "description too long ({d} bytes) — keep it under 2000\n";
pub const project_description_cleared = "project description cleared\n";
pub const project_description_set = "project description set\n";
pub const could_not_set_project_color = "could not set the project colour ({s})\n";
pub const could_not_rename_project = "could not rename the project ({s})\n";
pub const could_not_set_blank_color = "could not set the blank colour ({s})\n";
pub const could_not_set_description = "could not set the description ({s})\n";

pub const keywords_none = "keywords for \"{s}\": (none)\n";
pub const keywords_head = "keywords for \"{s}\":";
pub const could_not_update_keywords = "could not update keywords ({s})\n";
pub const could_not_query_server = "could not query {s} ({s})\n";
pub const no_project_named_anywhere = "no project named \"{s}\" on any connected server\n";
pub const could_not_read_keywords = "could not read keywords for \"{s}\" ({s})\n";
pub const keywords_usage = "usage: /keywords <project>\n";
pub const keywords_search_usage = "usage: /keywords-search <keyword...>\n";
pub const could_not_list_projects = "could not list projects on {s} ({s})\n";
pub const no_keyword_matches = "no projects match those keywords\n";
pub const keywords_edit_usage = "usage: /keywords-{s} <project | [\"a\",\"b\"]> <keyword...>\n";
pub const rename_needs_name = "give a new name — e.g. '/rename MyProject'\n";
pub const renamed = "renamed to \"{s}\"\n";
pub const invalid_duration = "invalid duration '{s}'\n";
pub const expiry_cleared = "expiration cleared — project kept forever\n";
pub const expires = "expires {s}\n";
pub const expire_usage = "usage: /expire <duration> — set when the active server project expires\n";
pub const expire_usage_unit = "  a unit alone (one of it): {s}\n";
pub const expire_usage_count = "  a count + unit (either order): 'days 23' | 'months 3' | '3 weeks'\n";
pub const expire_usage_off = "  keep forever: {s}\n";
pub const could_not_set_expiry = "could not set the project expiry ({s})\n";
pub const color_none = "{s}: (none — neutral grey)\n";
pub const no_connections = "no connections — '/connect <url>' first\n";
pub const fetch_usage = "usage: /fetch <name> [url] — e.g. '/fetch MyProject'\n";
pub const fetch_needs_url = "multiple servers connected — give a URL: '/fetch {s} <url>'\n";
pub const server_lookup_failed = "server lookup failed ({s})\n";
pub const no_project_named_on = "no project named \"{s}\" on {s}\n";
pub const could_not_download_image = "could not download image ({s})\n";
pub const could_not_decode_image = "could not decode server image ({s})\n";
pub const fetched_sync = "fetched \"{s}\" from {s} (sync {s})\n";
pub const sync_usage = "sync takes 'on', 'off', or nothing (to toggle)\n";
pub const sync_state = "sync {s}\n";
pub const sync_no_project = "  (no active server project yet — use '/fetch <name>')\n";
pub const chat_history_cleared = "chat history cleared\n";
pub const chat_file_deleted = "  (server chat file deleted)\n";
pub const chat_usage = "chat takes 'on', 'off', 'show', or 'clear'\n";
pub const chat_state = "chat {s} ({d} saved turns)\n";
pub const chat_on_note = "  saved into the .stencil project on /save; on a server project, readable by everyone it is shared with\n";

pub const script_usage = "usage: /script <directives…>  (';' separates statements, e.g. /script @crop 25%;@filter bw)\n";
pub const script_run_needs_path = "usage: /script-run <file.stc>\n";
pub const script_unreadable = "could not read that script\n";
pub const script_failed = "the script stopped partway — earlier edits stand\n";
pub const script_no_edits = "that script recorded no edit here\n";
pub const script_source_ignored = "/script ignores @source — the ops apply to the loaded image\n";
pub const script_save_ignored = "/script ignores @save — use '/save <path>' to write the result\n";
pub const script_frame_ignored = "/script ignores @frame — the console edits the loaded image\n";
pub const exec_usage = "usage: /exec <action> <args> — actions: crop | rotate | filter | apply (e.g. '/exec rotate 1')\n";
pub const crop_usage = "usage: /crop <spec> [album] — edges x1= x2= y1= y2= with %, px, cm/mm/in, or a bare pixel delta; omit an edge to keep the image bound\n";
pub const crop_usage_example = "       e.g. '/crop x1=10% x2=90% y1=10% y2=90%' (add 'album' to derive a missing axis from the page, landscape)\n";
pub const rotate_usage = "usage: /rotate <int> — quarter-turns: 1 = 90° cw, 2 = 180°, -1 = 90° ccw, 3 = 270° (e.g. '/rotate -1')\n";
pub const apply_needs_path = "apply needs a path or URL to a layout JSON — e.g. '/apply notes.json'\n";
pub const rotate_needs_int = "rotate needs an integer (quarter-turns), e.g. '/rotate -1'\n";
pub const rotate_full_turn = "rotate {d} is a full turn — no change\n";
pub const unknown_filter = "unknown filter \"{s}\" — 'bw', 'sepia', 'invert', 'contour', 'none', or a colour\n";

pub const reset_done = "reset to original\n";
pub const unknown_theme = "unknown theme '{s}' — type '/theme' to list them, or give a colour like #ff5623\n";
pub const theme_set = "theme set to {s} ({s})\n";
pub const mouse_full_screen_only = "mouse control is only available in --console-full-screen\n";
pub const mouse_usage = "usage: /mouse [on|off] (bare toggles) — off lets you select text\n";
pub const mouse_on = "mouse on — click the logo to theme, drag to select (Ctrl-S copies), wheel to scroll\n";
pub const mouse_off = "mouse off — you can select/copy text natively now; '/mouse on' to re-enable clicks\n";
pub const reveal_full_screen_only = "reveal speed is only available in --console-full-screen\n";
pub const reveal_usage = "usage: /reveal-speed [{d} … {d}] — {d} slowest, {d} instant (default {d})\n";
pub const reveal_instant = "{s} {d} — output appears instantly\n";
