import type { StencilElement } from './base.js';

/** The `<stencil-app-container>` shell: toolbar + selection panel + image info + main content. */
export declare class StencilAppContainer extends StencilElement {
  static inner(): string;
  static template(): string;
}
