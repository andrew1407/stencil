import type { StencilElement } from './base.js';

/** The idle "No image loaded" line above the canvas. */
export declare class StencilImageInfo extends StencilElement {
  static inner(): string;
  static template(): string;
}
