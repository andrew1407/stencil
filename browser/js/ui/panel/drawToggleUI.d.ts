import type { DrawingApp } from '../../core/drawingApp.js';

/** The Draw group's Start/Stop face; core holds no icon markup. */
export declare const syncDrawToggleUI: (app: DrawingApp) => void;
/** The Draw group's Line/Rect mode face. */
export declare const syncDrawModeUI: (app: DrawingApp) => void;
