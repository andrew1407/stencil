/** Removes the one app-wide hover magnifier, if shown. */
export declare const hideThumbPreview: () => void;

/** Wires the hover magnifier over one chat thumbnail image. */
export declare const wireThumbPreview: (img: HTMLImageElement, caption?: string) => void;
