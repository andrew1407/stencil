{
  "name": "blank-color-bogus-name-valid",
  "profiles": [
    "editor",
    "console",
    "bot",
    "mcp"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "blank",
        "color": "notacolour"
      }
    ]
  },
  "expect": "valid",
  "knownDivergence": {
    "desktop": "invalid"
  },
  "reason": "SUSPICIOUS: the CSS-name check is any-letters (/^[a-zA-Z]+$/) — an arbitrary alphabetic word passes validation and fails only at render; pinned as implemented. MEASURED DIVERGENCE: desktop validates blank color via core::parseColor (a real CSS-name table, opPlan.cpp isColorNameOrHex), so \"notacolour\" is rejected there"
}
