{
  "name": "forbidden-op-sharetabs",
  "profiles": [
    "all"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "shareTabs",
        "on": true
      }
    ]
  },
  "expect": "valid",
  "knownDivergence": {
    "mcp": "invalid"
  },
  "reason": "§13 forbidden op: the parser is not where every surface enforces it — browser/desktop/pystencil/bot/extension skip it as unknown at parse (valid plan, warning) and refuse at the EXECUTOR; cli and mcp hard-fail at parse where their lists carry the name. DRIFT: on browser/bot/mcp/extension lists, absent from cli — only mcp rejects at parse."
}
