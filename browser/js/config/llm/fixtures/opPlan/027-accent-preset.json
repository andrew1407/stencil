{
  "name": "accent-preset",
  "profiles": [
    "editor",
    "console"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "accent",
        "preset": "green"
      }
    ]
  },
  "expect": "valid",
  "reason": "preset form: browser/desktop/cli; the extension accent takes only \"color\" — excluded from its profile"
}
