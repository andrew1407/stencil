{
  "name": "filter-sepia",
  "profiles": [
    "editor",
    "console",
    "bot",
    "mcp"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "filter",
        "mode": "sepia"
      }
    ]
  },
  "expect": "valid",
  "reason": "NOTE: the extension registers a DIFFERENT op under the same name \"filter\" (its panel list filter) — this core shape is invalid there (unknown field \"mode\"); see filter-core-shape-in-extension"
}
