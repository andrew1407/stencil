{
  "name": "save-with-path",
  "profiles": [
    "editor",
    "console",
    "bot",
    "mcp"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "save",
        "name": "p",
        "path": "~/Downloads"
      }
    ]
  },
  "expect": "valid",
  "reason": "contract §2 gives save an optional \"path\" (string ≤ 1024 chars, never a URL); every surface carrying save accepts it — desktop and cli honour it under the user-echo guard, mcp honours it relative to the run's output_dir (traversal-guarded), browser, pystencil and bot accept it and note+skip the destination"
}
