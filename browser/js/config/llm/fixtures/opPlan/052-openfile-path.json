{
  "name": "openfile-path",
  "profiles": [
    "editor",
    "console"
  ],
  "input": {
    "version": 1,
    "reply": "ok",
    "actions": [
      {
        "op": "openFile",
        "path": "~/Pictures/cat.png"
      }
    ]
  },
  "expect": "valid",
  "reason": "openFile is desktop + cli only (contract §8); browser and pystencil skip it as unknown — same verdict, different effect"
}
