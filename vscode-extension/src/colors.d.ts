// Shape of colors.js — the command that opens VS Code's token-colour setting.
export declare const SETTING: string;
export declare const DEFAULT_RULES: Record<string, string>;
export declare const configureColors: () => Promise<void>;
export declare const register: (context: unknown) => unknown;
