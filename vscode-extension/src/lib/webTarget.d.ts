// Shape of webTarget.js — the one resolution of the browser instance's URL.
export declare const BAD_WEB_URL: string;
export declare const normalizeWebUrl: (value: unknown) => string;
export declare const webUrlFor: (vscode: unknown) => string;
