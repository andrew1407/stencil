// Shape of extension.js — the activation entry point VS Code calls.
export declare const activate: (context: { subscriptions: { dispose(): void }[] }) => void;
export declare const deactivate: () => void;
