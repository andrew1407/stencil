<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Stencil.TelegramBot.Domain\Stencil.TelegramBot.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.9" />
  </ItemGroup>

  <ItemGroup>
    <!-- Canonical §4 system-prompt prose, embedded so the bot needs no repo checkout at run time. -->
    <EmbeddedResource Include="../../../browser/js/config/llm/systemPrompt.json" Link="Assets/systemPrompt.json" />
    <!-- The shared op registry the plan validator is table-driven from (llm-contract.md §1–§13). -->
    <EmbeddedResource Include="../../../browser/js/config/llm/opRegistry.json" Link="Assets/opRegistry.json" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
