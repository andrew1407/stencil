{
  "_doc": "Size + comment ratchet. New files must stay under maxNewFileLines. Listed files must not grow. Comment share per directory must not rise. Lower these numbers as the refactor lands; never raise one without a note in 'exceptions'. Rust files carry inline #[cfg(test)] modules, so an entry is either a bare line count or {lines, prod}, where prod is the part before that module and is what maxNewFileLines applies to.",
  "maxNewFileLines": 230,
  "maxFilesPerDir": 12,
  "dirs": {
    "mcp/tests": 50
  },
  "files": {
    "mcp/src/server/mod.rs": {
      "lines": 120,
      "prod": 118
    },
    "mcp/src/server/tools/prompt/response.rs": {
      "lines": 282,
      "prod": 88
    },
    "mcp/tests/layout_test.rs": 175,
    "mcp/tests/registry_test.rs": 196,
    "mcp/tests/test_floor_test.rs": 121
  },
  "commentPct": {
    "mcp/src": 19,
    "mcp/src/args": 19,
    "mcp/src/config": 14,
    "mcp/src/deliver": 12,
    "mcp/src/llm": 16,
    "mcp/src/llm/providers": 8,
    "mcp/src/llmtransport": 13,
    "mcp/src/opplan": 13,
    "mcp/src/opplan/lower": 12,
    "mcp/src/opplan/schema": 5,
    "mcp/src/pipeline": 15,
    "mcp/src/server": 10,
    "mcp/src/server/tools": 8,
    "mcp/src/server/tools/prompt": 9,
    "mcp/tests": 8,
    "mcp/tests/common": 7
  },
  "exceptions": {},
  "_notes": {
    "mcp/src commentPct 19 -> 21": "Splitting config.rs into config/ moved 319 comment-sparse lines out of mcp/src. The directory's ABSOLUTE comment count FELL; only the denominator moved. No comment was added.",
    "mcp/src/opplan commentPct 16 -> 17": "Splitting lower.rs into opplan/lower/ moved 351 comment-sparse lines out of mcp/src/opplan. The directory's ABSOLUTE comment count FELL; only the denominator moved. No comment was added.",
    "mcp/src commentPct 21 -> 23": "Splitting deliver.rs into deliver/ moved 251 comment-sparse lines out of mcp/src. The directory's ABSOLUTE comment count FELL; only the denominator moved. No comment was added.",
    "mcp/src/server/tools/prompt/response.rs": "90 production lines; the rest is the inline #[cfg(test)] mod tests, which this ratchet measures separately (see _doc). Listed so the test half cannot grow either.",
    "mcp/src/server/mod.rs 140 -> 122": "The #[cfg(test)] testwire helpers moved to src/server/testwire.rs, so the file shrank even as stencil_script's #[tool] landed in it."
  },
  "_dirsNote": "tests/ mirrors its source folder for folder. A module commonly carries several test files, so a mirrored test folder can sit above the source cap; helpers/ is shared by the whole suite and has no one feature, and Cargo can only see an integration test directly in tests/. These are frozen at today's counts and may only shrink."
}
